This program simulates a mini library inventory system.

It:

Creates 120 “Book” records (each with a name and ID).

Sorts them alphabetically by name.

Builds a separate index sorted by ID.

Lets the user enter an ID to search for.

Uses binary search to find the book quickly.

Cleans up memory afterward.
